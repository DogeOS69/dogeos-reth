# Reth 2 extension-point capability matrix

This is the Phase 0 spike checklist. “Unverified” means that no copied
upstream implementation may be used to satisfy the row.  The result must be
changed to **public hook**, **generic patch**, or **unsupported** by a minimal
standalone Reth 2 / REVM 36 spike.

The dependency-family gate is complete; see `DEPENDENCY_AUDIT.md`. Verified capabilities below
are compiled and unit-tested through public Reth APIs. The Engine and Storage V2 rows also have a
real-node transcript in `ENGINE_V1_SMOKE.md`.

| Requirement | Oracle owner / evidence | Standalone DogeOS owner | Reth 2 status |
| --- | --- | --- | --- |
| DogeOS-owned `NodeTypes` composition boundary | New `crates/dogeos-reth-node` | `dogeos-reth-node` | Public `Node`/component hooks; DogeOS primitives, chainspec, Engine payload types, transaction-bound storage, concrete component graph, `eth_` RPC builder, and Engine/RPC add-on graph are assembled as a launchable preset and type-tested |
| Standalone node CLI and chain selection | `bin/reth`, Scroll chain parser | `bin/dogeos-reth` | Public Reth CLI and node-launch hooks; `dogeos-mainnet`, `dogeos-chikyu`, `dev`, and custom genesis inputs select `DogeosChainSpec`, then launch the DogeOS node preset with sequencer forwarding and payload-size policy arguments |
| Custom transaction and receipt encodings | `crates/scroll/primitives`, `crates/scroll/alloy/consensus` | `dogeos-protocol-types` + `dogeos-reth-primitives` | Public traits; canonical protocol tests, Reth compact round trips, and MDBX `Compress`/`Decompress` bindings pass |
| Feynman and Tsuki fork policy | `crates/scroll/alloy/hardforks`, `crates/scroll/hardforks` | `dogeos-hardforks` | Public hook; unit-tested |
| DogeOS mainnet, Chikyu, and dev chainspecs | `crates/scroll/chainspec/{dogeos,chikyu,dev}.rs` | `dogeos-chainspec` | Public `ChainSpec`/`EthChainSpec` hooks; Feynman+ schedules and Chikyu hash unit-tested |
| Native DOGE and Tsuki state transition | `crates/scroll/alloy/evm/src/block/tsuki.rs`, `revm-scroll` | `dogeos-reth-evm` + `revm-scroll` | Public REVM state API; insert/no-overwrite/idempotence tests pass |
| L1 fee and stateful base-fee policy | `crates/scroll/{evm,consensus,txpool}` | `dogeos-reth-evm`, `dogeos-reth-txpool`, `dogeos-reth-rpc`, `dogeos-reth-node` | Public `BlockExecutorFactory`/`ConfigureEvm`, transaction-validator, provider/cache, `EthFees`, and RPC replacement hooks; L1 fee receipts, canonical compression, state-aware payload base fee, Tsuki fee caps, balance buffering, transitions, EIP-2935 calls, payload-byte-aware `eth_maxPriorityFeePerGas`, and Scroll-compatible `eth_feeHistory` next-base-fee/reward substitution are verified |
| Feynman+ block execution and assembly | `crates/scroll/{alloy/evm,evm}` | `dogeos-reth-evm` | Public Alloy EVM `StateDB`, executor-factory, Reth `ConfigureEvm`, and `BlockAssembler` hooks; workspace tests pass |
| Engine types, payload attributes, build and validation | `crates/scroll/{engine-primitives,payload,node}` | `dogeos-reth-engine`, `dogeos-reth-payload`, `dogeos-reth-evm` | Public `EngineTypes`/`PayloadTypes`/`BuiltPayload`/`PayloadBuilder`/`ConfigureEngineEvm` hooks; attributes, version-domain-compatible Scroll V1 payload IDs, Scroll payload environments, transaction decode/recovery, forced/pool execution, execution cache, synchronous state-root fallback, built payloads, and Engine validation compile and test. A real node completed `forkchoiceUpdatedV1 -> getPayloadV1 -> newPayloadV1 -> forkchoiceUpdatedV1`, returned a `0x01`-domain payload ID, and committed the resulting block. Legacy `BlockDataHint` remains encoded and payload-ID-significant but no longer overrides post-Euclid consensus or execution-derived header fields. Sparse-trie state-hook sharing remains a deferred optimization, as allowed by the correctness migration scope |
| Post-Euclid consensus and forced transaction ordering | `crates/scroll/{consensus,payload}` | `dogeos-reth-consensus`, `dogeos-reth-engine`, `dogeos-reth-payload` | Header validation and Engine payload reconstruction are fixed to post-Euclid semantics (zero coinbase/nonce/mix hash, difficulty one, empty extra data, no future timestamps) with no Clique difficulty-two fallback, while consensus and Engine validators allow equal parent timestamps; forced transactions decode once, retain order/bytes, reject trailing bytes, execute before pool transactions, and freeze `noTxPool` payloads |
| Storage V2 body reconstruction (`ommers=[]`, `withdrawals=None`) | Current Scroll node aliases `EthStorage<ScrollTransactionSigned>` | `dogeos-reth-node` | Public generic `EthStorage<T>` hook retained without copied Reth code; Feynman+ keeps Shanghai EVM behavior while the Engine boundary and local payload attributes explicitly omit unsupported EIP-4895 withdrawals. A clean restart reopened the Engine-imported canonical block from the same Storage V2 datadir with the same block/state roots |
| RPC receipt conversion and `debug_executionWitness` | `crates/scroll/rpc`, `crates/scroll/alloy/rpc-types` | `dogeos-rpc-types` + `dogeos-reth-rpc` + `dogeos-reth-node` | Scroll network/request/transaction/receipt schemas, signing, `l1Fee`, receipt metadata, sender recovery context, receipt-derived transaction metadata, simulation and REVM call-environment conversion are assembled on public Reth hooks; the node `eth_` builder, equal-timestamp pending environment, HTTP/WS sequencer client, exact raw-transaction encoding, local-pool-first asynchronous/synchronous forwarding policy, fail-closed message-queue witness state loading, and public-hook replacement of both execution-witness RPC methods are verified. A real `debug_executionWitnessByBlockHash` response included the message-queue account plus withdraw-root slot `0xb` and next-message-index slot `0xc` |
| Txpool fee policy and `noTxPool` | `crates/scroll/txpool`, `crates/scroll/node` | `dogeos-reth-txpool`, `dogeos-reth-payload`, `dogeos-reth-node` | Custom pooled transaction, exact encoded-byte cache, L1 fee validation, blob/L1-message rejection, maintenance tasks, pool bypass, resource limits, and deterministic `noTxPool` freeze use public hooks and compile together |
| Block import/listening and `scroll-wire` signed blocks | `crates/scroll/node` plus `dogeos-rollup-node/crates/scroll-wire` | `dogeos-reth-node` + `dogeos-scroll-wire` | Public `NetworkBuilder`, `NetworkConfigBuilder::add_rlpx_sub_protocol`, `EthApiCtx::engine_handle`, and `ConsensusEngineHandle::new_payload` hooks verified; the launched node enables `scroll/1` by default, hands its event manager into a critical Engine-import task, and exposes compatible `network.scroll-wire`/`network.valid-signer` policy flags. Capability identity, message ID, RLP field order, raw-parity signature bytes, signature hash/authorization, peer tracking, announcements, bounded `(blockHash, signature)` duplicate suppression, DogeOS block-to-payload conversion, and Engine import are tested. Forkchoice remains owned by the consensus/orchestration layer |
| Fresh V2 replay and abrupt-stop recovery | No standalone implementation | `dogeos-reth-node` | Public node/storage hooks verified on an isolated dev chain: fresh Storage V2 initialization, local payload production, clean restart, and SIGKILL recovery all reopened MDBX/RocksDB/static files through Reth consistency checks and resumed canonical block production. The crash test recovered the last durable head (48), discarded the in-flight block, and advanced past it. Full Chikyu replay/root parity remains a qualification gate, not an extension-point gap |

## Spike acceptance criteria

For each row, record the public trait/module used, a minimal compile target,
and the associated test.  A “generic patch” entry must be added to
`UPSTREAM_PATCHES.md` before it can be used.  An “unsupported” result blocks
extraction until an upstream-generic extension is proposed.
